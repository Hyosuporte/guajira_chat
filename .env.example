# Server Configuration
PORT=3000

# Database Configuration (PostgreSQL)
# Ensure pgvector extension is installed for embeddings
DB_HOST=localhost
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=your_database

# AI Services
OPENAI_API_KEY=sk-proj-...

# Embeddings Cache (Optional)
# The project uses local_cache/fast-bge-small-en
